import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.vetevcharger',
  appName: 'vet-ev-charger',
  webDir: 'dist'
};

export default config;
